9장 파일 I/O
1절. txt파일 io
	with open(파일상대경로|파일절대경로,  'r|w|a|rt|wt|at', encoding) as f:
		list변수 = f.readlines() / 문자변수 = f.read()
		f.write(저장할 문자열) / f.writelines(문자list)
2절. 피클(pkl, pickle, data) 파일 io
	with open(파일경로, 'rb|wb') as f:
		변수 = pickle.load(f)
		pickle.dump(변수, f)
3절. CSV파일 io
	with open(파일경로, 'r|w|a|rt|wt|at', encoding) as f:
		reader = csv.reader(f); 2차원list = list(reader)
		writer = csv.writer(f); writer.writerows(2차원list)

		dic_reader = csv.DictReader(f); 딕셔너리list = list(dic_reader)
		dic_writer = csv.DictWriter(f); dic_writer.writerows(딕셔너리list)
4절. JSON파일 io
	with open(파일경로, 'r|w|rt|wt|a|at', encoding) as f:
		딕셔너리list - json.load(f) 
		json.dump(딕셔너리list, f, ensure_ascii=False, indent)

		객체list = json.load(f, object_hook=딕셔너리를객체로return하는함수명)
		json.dump(객체list, f, default=클래스명.객체를딕셔너리로return함수명)
5절. h5/hdf5 파일 io
	import pandas as pd
	데이터프레임변수 = pd.read_hdf(파일경로, key)
	데이터프레임변수.to_hdf(파일경로, key)











